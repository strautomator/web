// STRAUTOMATOR WEB SETTINGS
// These are the general settings for the Strautomator Web frontend. There's also a settings.development.json
// and a settings.production.json, each one containing environment-specific settings. Secret settings are
// defined on the settings.secret.json or via environment variables.
//
// Please note that the vast majority of the Strautomator's settings are defined and described on the Core
// project, at https://raw.githubusercontent.com/strautomator/core/master/settings.json

{
    "api": {
        // API is rate limited to 600 requests per minute per instance by default. If you wish to disable
        // rate limiting altogether, set "rateLimit": false.
        "rateLimit": {
            "max": 600,
            "windowMs": 60000
        },
        // Only accept connections coming via Cloudflare by default.
        "requireCloudflare": true
    },
    "app": {
        // The server will look for the files strautomator.cert and strautomator.key on the
        // app root to start a HTTPS server on port 8443. If these files are not there,
        // the server will listen on HTTP port 8080 instead.
        "port": null,
        // You can set the app URL on your settings.secret.json file or
        // via the $SMU_app_url environment variable.
        "url": "https://strautomator.com/",
        // Listen to all incoming connections by default.
        "ip": "0.0.0.0",
        // Express trust proxy settings.
        "trustProxy": 1
    },
    "webhooks": {
        // Retry the webhooks setup every 60 seconds (if failed). Value in milliseconds.
        "retryInterval": 60000
    }
}
