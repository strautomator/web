// STRAUTOMATOR WEB SETTINGS (DEVELOPMENT)

{
    "api": {
        // Base URL to the API must be set via the settings.private.json file or
        // via the $STA_WEB_api_url environment variable.
        "url": null
    },
    "app": {
        // Default port for web on development is 3000.
        "port": 3000,
        // Please set the app URL on your settings.private.json file or
        // via the $STA_WEB_app_url environment variable.
        "url": "http://strautomator.local/",
        "title": "Strautomator DEV"
    }
}
