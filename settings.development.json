// STRAUTOMATOR WEB SETTINGS (DEVELOPMENT)

{
    "api": {
        // Secure token that should be passed as a "Strautomator-Key" header
        // on API requests marked for administrators only.
        "adminToken": "Dev",
        // You can set this to true on development to skip OAuth2 header checks.
        "skipAuthValidation": false
    },
    "app": {
        // Default Web port for web on development is 3000.
        "port": 3000,
        // Please set the app URL on your settings.private.json file or
        // via the $STA_app_url environment variable.
        "url": "http://strautomator.local/",
        "title": "Strautomator (Dev)"
    }
}
